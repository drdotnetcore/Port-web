@page
@model Port_web.Pages.Products.IndexModel
@{
}
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Product List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-page="/Products/Create" class="btn btn-primary"><i class="bi bi-plus-circle-fill"></i> &nbsp; Create</a>
        </div>
    </div>
    <br /><br />
    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Product Name
                </th>
                <th>
                    Display Order
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach(var objCat in Model.Product)
            {
                <tr>
                    <td width="30%">@objCat.Name</td>
                    <td width="30%">@objCat.Name</td>
                    <td width="10%">
                        <div class="w-75 btn-group" role="group">
                        <a asp-page="Edit" asp-route-id="@objCat.Id" class="btn btn-primary mx-2">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a asp-page="Delete" asp-route-id="@objCat.Id" class="btn btn-danger mx-2">
                            <i class="bi bi-trash-fill"></i>
                        </a>
                        </div>

                    </td>
                </tr>
            }
        </tbody>
    </table>
    
</div>
